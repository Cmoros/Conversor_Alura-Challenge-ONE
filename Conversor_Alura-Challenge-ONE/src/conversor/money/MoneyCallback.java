package conversor.money;

import interfaces.CallBack;

public class MoneyCallback implements CallBack {
  public String getResultText(double quantity, String from, String to) {
    // String fromCode = MoneyConversorVisual.conversor.code(from);
    // String toCode = MoneyConversorVisual.conversor.code(to);
    // double result = MoneyConversorVisual.conversor.convert(quantity, fromCode,
    // toCode);
    // to = MoneyConversorVisual.conversor.plurarize(to);
    // return "Tienes $" + String.valueOf(result) + " " + to;
    return "";
  }
}
